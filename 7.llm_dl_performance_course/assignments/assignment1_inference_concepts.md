# Assignment 1: Inference Concepts (Part 1)

**Complete after Part 1 (Modules 1–5).**

---

## Objective

Demonstrate that you understand core inference concepts and can explain them in your own words, with one biology-relevant example.

---

## Task

1. **Explain in 2–3 paragraphs (total):**
   - What **batching** is and why it matters for inference.
   - What **memory management** in inference means (e.g. weights, activations, KV cache) and why it matters.
   - How **GPU saturation** relates to throughput and cost.

2. **Biology example:** Give one concrete example of how batching or memory constraints could apply when running a gene-language model or cell-embedding model (e.g. “when embedding 1M cells, batching allows…” or “memory limits batch size to…”).

---

## Deliverable

- A short document (≈1 page or equivalent) with your explanations and the biology example.
- Save to your `results/` or `notes/` folder (e.g. `assignment1_inference_concepts.md` or `.pdf`).

---

## Self-check

- [ ] Used your own words (no copy-paste from slides only).
- [ ] Biology example is specific (model type + batching or memory).
- [ ] You could explain your answers in a 2-minute verbal summary.
